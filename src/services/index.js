const usersServices = require('./user.services');
const userCategories = require('./categories.services');
const postServices = require('./post.services');

module.exports = {
    usersServices,
    userCategories,
    postServices,
};